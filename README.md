
# × ×
# batsu

Functional CSS-in-JS for React

```sh
npm i batsu
```

```jsx
import React from 'react'
import Base from 'batsu'

const Box = props =>
  <Base
    {...props}
    css={{
      color: 'tomato'
    }}
  />
```
